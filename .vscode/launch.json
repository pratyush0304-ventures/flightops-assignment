{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "SkyVault: current file (use this file's folder)",
      "type": "debugpy",
      "request": "launch",
      "program": "${file}",
      "cwd": "${fileDirname}",
      "console": "integratedTerminal"
    }
  ]
}
